"""Scripts utilitários avulsos (manutenção, migração, verificação).

Diferente de `experiments/` (que roda um modelo/pipeline completo),
`scripts/` reúne utilitários pontuais — por exemplo, o futuro script de
migração incremental de cada notebook para `src/` (ver
ARCHITECTURE_AUDIT.md, seção 8, fases 1 e 6).

Nenhum script é executado por este pacote nesta etapa.
"""
