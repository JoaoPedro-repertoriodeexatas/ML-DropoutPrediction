"""Pontos de entrada de linha de comando para treinar/avaliar modelos.

Responsabilidade futura: um `run_model.py` (``python -m experiments.run_model
--model arvore_de_decisao``) e um `run_all.py` que treinam via `src/`,
substituindo o padrão atual em que só o Naive Bayes é executável fora de
um notebook (`models/naive_bayes/train.py`) — ver ARCHITECTURE_AUDIT.md,
seção 7.1.

Nenhum experimento é executado por este pacote nesta etapa.
"""
